[project]
name = "xaue-mcp"
version = "0.1.0"
description = "A minimal Python MCP server demo."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp",
]

[project.scripts]
xaue-mcp = "main:run"
